// you can use the parseInt() function to parse a string and return an interger. If the first character in the string isnt a number, then it will return NaN

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
