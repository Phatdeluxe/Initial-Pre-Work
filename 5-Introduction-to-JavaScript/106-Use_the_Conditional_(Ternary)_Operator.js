// instead  of using an if/else statement you can use the conditional operator. the format is: condition ? statement-if-true : statement-if-false;

function checkEqual(a, b) {
  return a != b ? false : true;
}

checkEqual(1, 2);
