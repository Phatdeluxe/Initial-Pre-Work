// when assigning a variable, the computer reads it left to right, so everyting on the right of the = sign is resolved before it is assigned to a variable

// Setup
var a;
var b = 2;

// Only change code below this line

a = 7;
b = a;
