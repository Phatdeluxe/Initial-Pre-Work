// If the name of the property has a space in it, then you need to use bracket notation. Inside the brackets you need to surround the entire name in either single or double quotes

// Setup
var testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Only change code below this line

var entreeValue = testObj["an entree"];   // Change this line
var drinkValue = testObj["the drink"];    // Change this line
